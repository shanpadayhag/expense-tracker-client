import 'package:flutter/material.dart';

class FundBalance extends StatelessWidget {
  final String fundName;

  const FundBalance({
    Key? key,
    required this.fundName,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
